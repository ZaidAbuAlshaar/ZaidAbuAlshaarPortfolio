# Copy to .env.local for local development.
# Both values are public (safe to expose in the client bundle); the security
# boundary is Supabase Row Level Security: public read, writes locked to the
# admin account. supabase.ts also ships safe defaults, so these are optional.
VITE_SUPABASE_URL=https://kgbsmxcezmkatcevhkzs.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
